{
  "name": "mestre-test",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run build && wrangler pages dev dist",
    "build": "bun scripts/build.ts",
    "preview": "bun run build && bunx serve dist",
    "deploy": "bun run build && wrangler pages deploy",
    "deploy:list": "wrangler pages deployment list --project-name mestre-test"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
